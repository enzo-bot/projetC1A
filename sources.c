#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

boolean verifierAlphaNumerique(char *texte, char *texteVerifie) {

	for (int i = 0 ; i < strlen(texte); i++)
	{
		if (isanum == 0){
			return false;
		}
	}
	return true;
	fclose(fichier);
}